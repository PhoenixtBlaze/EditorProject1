// FlameCore placeholder bundle for "runtime.js".
// The in-browser pipeline cannot compile JS; run "npm run build:site" to
// produce a real bundle (built by Vite from src/pipeline/exported-site/main.ts).
document.body.innerHTML = '<pre style="color:#fff;padding:1em;font-family:monospace">' +
  'FlameCore placeholder bundle. Build a real one with: npm run build:site' +
  '</pre>';
